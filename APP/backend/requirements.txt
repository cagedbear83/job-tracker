# Runtime dependencies for the Illinois UI Job Tracker backend.
#
# Only direct (top-level) imports are pinned here; pip resolves the
# transitive tree. This replaced a 105-line `pip freeze` dump that shipped
# many packages the app never imports (localstack, docker, llama-cpp-python,
# scipy, h5py, numpy-quaternion, webpack, resend, reportlab, ...), bloating
# install time, image size, and attack surface.

# --- Web framework / server ---
fastapi==0.136.3
uvicorn==0.48.0
python-multipart==0.0.29          # multipart form + file uploads (CSV/screenshot import)

# --- Data / validation ---
motor==3.7.1                      # async MongoDB driver (pulls pymongo)
pymongo==4.17.0                   # used directly by admin/maintenance scripts + tests
pydantic==2.13.4
email-validator==2.3.0            # required by pydantic EmailStr

# --- Auth / security ---
bcrypt==5.0.0
PyJWT==2.13.0                     # NOTE: PyJWT, not the unrelated `jwt` package

# --- Scheduling / time ---
APScheduler==3.11.2
pytz==2026.2

# --- Integrations ---
requests==2.34.2                  # Mailgun HTTP API + helper scripts
twilio==9.10.9                    # SMS reminders / OTP
google-generativeai==0.8.6        # Gemini screenshot OCR
pillow==12.2.0                    # image handling for screenshot import
stripe==11.1.0                    # Stripe payment processing

# --- Reporting ---
pypdf==6.13.0                     # fills the IDES ADJ034F AcroForm template

# --- Config ---
python-dotenv==1.2.2

# --- Production hardening ---
slowapi==0.1.9                    # per-IP rate limiting on auth endpoints
sentry-sdk[fastapi]==2.20.0       # error tracking (no-op unless SENTRY_DSN set)

# cache bust 20260812172107

# cache bust 20260813144640
